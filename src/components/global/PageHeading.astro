---
type Props = {
  title: string;
  description?: string;
};
const { title, description } = Astro.props;
---

<div class="">
    <h1 class="text-2xl text-teal-900 font-bold mb-5">{title}</h1>
    {description && <p class="text-gray-600">{description}</p>}
</div>